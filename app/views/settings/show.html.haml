- content_for :title, "Setting"
- content_for :header do
  .row.mb-2
    .col
      %h1.m-0.text-nowrap
        Setting

    .col
      %ol.breadcrumb.float-sm-right
        %li.breadcrumb-item
          = link_to root_path do
            %i.fas.fa-home
            Home
        %li.breadcrumb-item.active
          %i.fas.fa-gear
          Setting

.container-data_sources= render "data_sources"

.container-masked_data= render "masked_data"

.container-ignored_tables= render "ignored_tables"
