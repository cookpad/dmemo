- content_for :header do
  %h1.memo_title Home

.box
  .box-header.with-border
    %h2.box-title Databases

  .box-body
    %table.table.table-hover.table-bordered.table-striped{ role: "grid" }
      %thead
        %tr
          %th Database
          %th Description
          %th Tables
      %tbody
        - @database_memos.each do |database_memo|
          %tr
            %td
              = link_to database_memo.name, database_memo_path(database_memo.name)
              - unless database_memo.linked?
                %i.fa.fa-exclamation-triangle{ data: { toggle: "tooltip", placement: "bottom", title: "There is no linked source database." } }
            %td
              - if database_memo.description.present?
                = database_memo.description_text.truncate(30)
            %td
              = database_memo.table_memos.map(&:name).join(", ").truncate(100)
